<!DOCTYPE html>
<html>
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Napló</title>
        <link rel="stylesheet" href="/node_modules/js-datepicker/dist/datepicker.min.css">
        <link rel="stylesheet" type="text/css" href="/css/main.css">
        <link rel="stylesheet" type="text/css" href="/css/slick.css"/>
        <link rel="stylesheet" type="text/css" href="/css/slick-theme.css"/>

    </head>
    <body <%= typeof hatter !== 'undefined' ? 'class='+hatter : "" %>>
    <nav class="navbar navbar-default">
        <!--<div class="navbar-header">-->
            <!--<a class="navbar-brand" href="/">Napló</a>-->
        <!--</div>-->
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav main-menu">
                <li><a class="btn menu" href="/">Kezdőlap</a></li>
                <li><a class="btn menu" href="/pergetonaplo">Pergetőnapló</a></li>
                <li><a class="btn menu" href="/mucsali">Műcsalik</a></li>
                <li><a class="btn menu" href="/kep">Képek</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <% if(!currentUser){ %>
                    <li><a class="btn btn-yellow" href="/login">Bejelentkezés</a></li>
                    <li><a class="btn btn-yellow" href="/register">Regisztáció</a></li>
                <% } else { %>
                    <li><a class="btn btn-yellow" href="#">Üdv, <%= currentUser.username %></a></li>
                    <li><a class="btn btn-yellow" href="/logout">Kijelentkezés</a></li>
                <% } %>
            </ul>
        </div>
    </nav>
    <div class="container">
        <% if(errorMes && errorMes.length > 0){ %>
        <div class="alert alert-danger" role="alert">
             <%=errorMes%>
        </div>
        <% } %>
        <% if(successMes && successMes.length > 0){ %>
        <div class="alert alert-success" role="alert">
            <%=successMes%>
        </div>
        <% } %>
    </div>